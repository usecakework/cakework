# Building webpack module
`npm run build`

In a browser scenario where the library will be added using a script tag, the code will bind to global object window using the name from browserConfig.output.library.
