package flyApi

// Q: should this return machine info?
func NewMachine(userId string, project string, task string, flyApp string, name string, image string) error {
	// make a post request to the internal fly api endpoint
	return nil
}